In the provided map function, handling the text segment at index % 3 === 2 is omitted, as it represents the displayed URL text within the anchor tag, which isn't necessary when using the Microlink component. This avoids duplicate rendering of the URL text.
